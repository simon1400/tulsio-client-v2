export type WhoType = 'human' | 'dog' | 'cat'
export type StateType = 'looking' | 'has'

export interface CalculatorState {
  kg: number
  roztok: number
  objem: number
  lavel: number
  lavelState: number
}
