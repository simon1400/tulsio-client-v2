export const base = (theme: any) => ({
  styleOverrides: {
    html: {
      overflowX: "hidden",
    },
    body: {
      overflowX: "hidden",
      position: 'relative',
      minHeight: "100vh",
      width: '100%',
      background: "#202020"
    }
  }
})